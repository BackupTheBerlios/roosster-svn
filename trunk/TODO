
GUI
========
* make working with index faster by keeping Index* objects open
* add menu 
* let user specify index position at runtime through menu
* add help and about system
* find nice help icon (question mark?) and provide first tips and help
  via it's tooltip; give every tab it's own one
* use i18n feature of thinlet
* make it a native startable app for MacOS X with a decent menubar
* add public/private to table
* make tags editable (in dialog)
* decode HTML entities in GUI
* let simpy and del.icio.us sync run via dialog in bg
* finish edit tab (content, cached ...)
* sort search result
* integrate with clipboard
* make urls clickable so they open in browser

General
=======
* find way to let web server run in the background 
* implement custom close of GUI, so that http server gets stopped
  properly
* secure http server, make it only accessible from localhost

